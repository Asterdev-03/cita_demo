export const metadata = {
  title: "CITA | Dashboard",
  description: "Conversational Interview and Training Application",
};

const DashboardLayout = ({ children }) => {
  return <section>{children}</section>;
};

export default DashboardLayout;
